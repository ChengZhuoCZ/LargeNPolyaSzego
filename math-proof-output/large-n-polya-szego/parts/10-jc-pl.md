# JC_PL: Pathwise LCA Payment

## Local Summary
- Statement: Uniform nonnegativity of the completed pathwise action on every active no-ratio support chart
- Dependencies: Definitions
- Current obstruction: no proof is known for the full ratio-free pathwise action; super-near completed-Hessian control and coarse/nonregular localization remain unresolved in the retained source.
- Proof status: todo
- Next action: create a conjecture-portfolio route scout and submit the selected bounded core probe to independent review
- Local Context Packet: exact JC_PL statement + completed-action definitions + PSF-01--PSF-07 + `scratch/route-history.md`; no conditional-frontier audit is a proof receipt.

## Statement
There are no-ratio fan and radial endpoint-chart thresholds, and constants independent of $N$, of the smallest positive fan angle, and of adjacent angle ratios, such that the following holds after the endpoint modulus is reduced so that $\varepsilon_V(S)\le\kappa/2$. For every positive fan $\alpha$ in that neighborhood and every normalized support vector $z\in\mathsf X_\alpha$ whose full fixed-normal segment is active and satisfies the retained JP--NS chart bounds,
\[
h_\alpha\mathbf 1+[0,1]z\ \text{is active},\qquad
\sup_{0\le t\le1}\|\rho_t\|_{W^{1,\infty}}
+\bigl(\mathcal G^{\mathrm{JP}}_\alpha(z)\bigr)^{1/2}
\le C(S+\rho_0),
\]
one has
\[
\mathscr A_\alpha(z)\ge0.
\]
Here $\mathscr A_\alpha$ is the exact completed action defined in the retained joint-comparison source; its disk forcing, true ground-state multiplier, reduced-energy, mixed-flux, area, fixed-rank, and integrated polygonal Hessian terms are evaluated with one common completed covector. Any proof must remain valid when a genuine side tends to zero, integrate in support time before taking a side-ratio supremum, and count terminal, nonterminal, same-cell, adjacent, and first-separation pairs in one disjoint complete ledger.

## Dependencies

- `Definitions`: imports the fixed-normal support chart, the completed action, the fan defect, support metric, disk corrector, genuine-side convention, and PSF checklist.
- Retained source interface (not a blueprint dependency): Contract JC--PL in the retained conditional assembly is the migration source for the displayed claim and exact algebraic definitions.

## Proof Strategy
Status: todo
- Plan: Kind: mechanism — preserve the completed scalar and search at least two incompatible mechanisms before committing: the existing pathwise pair-ledger route and a global/localization bypass. The current Route Screen is tentative, its probe is `not-probed`, and the next valid move is conjecture-portfolio scouting rather than local proof drafting.
- Key obstacle: uniform coercivity must survive both the super-near moving completed Hessian and the coarse regime with collapsing sides; estimating the covector and Hessian separately destroys required cancellation.
- Needed dependencies: only frozen definitions are canonical at this pre-gate stage; every additional analytic interface must be exposed by a reviewed strict split.
- Failure trigger: statement likely false if an exact active-path counterexample gives negative action; route-level defect if every pair-ledger realization repeats the same nonuniformity without a strict reduction; constant-estimate insufficient if a bound loses powers of the regular angle.

## Experiments and Edge Cases
Status: todo

### Check 1: retained obstruction families
- Model: regular period-three side collapse, arbitrary active microscopic gap simplices, and genuinely nonregular fans with one short side.
- Method: retain the exact counterexample and floating-point searches only as falsification/calibration inputs; do not promote sampled positivity.
- Result: the old fixed-reserve RF--IM statement is refuted in the retained source, while no exact counterexample to JC_PL itself is recorded.
- Interpretation: inconclusive; next action compare genuinely different route families and bind a strict core reduction.

## Counterexample Search
Status: todo

The current search must include exact active paths approaching a zero side, high-frequency quotient directions, and configurations where support-time corner atoms collide. Floating-point scans may nominate witnesses but cannot close or refute the statement.

## Proof
Status: todo

No proof is claimed at migration. The prior conditional-frontier text identifies candidate subcontracts but does not discharge this quantified statement.

## Proof Attempt Log
Active attempt: none
Recent failed attempt: none
Superseded attempts: none

## Local Audit
Status: todo
