# Crux Dossier

Family ID: F-QRCI-TRACE-SPACE

## Core Obligation

Replace the false product-edge lifting and the undefined one-family
common/jump coefficient convention in JC_SP_REG by a typed material boundary
interface for both polygon families occurring in the exact completed scalar.

## Exact First Failure

The displayed map
\[
L:\prod_i H^{1/2}(0,1)\longrightarrow H^1(P_0)
\]
cannot be a right inverse of the boundary trace on arbitrary product-edge
data: traces of one global \(H^1(P_0)\) function satisfy cross-edge
compatibility at every vertex.  In addition,
\((A_t-\lambda_tM_t)Lf\) is ill-typed when \(A_t,M_t\) have only been
declared on \(H_0^1(P_0)\).  Finally, one polygon family cannot define the
two common/jump amplitudes demanded by the stated coefficient vector; those
coordinates must be formed from the completed scalar's \(Y_t\) and \(B_t\)
families.

## Minimal Repair

Use the actual material trace space
\(\operatorname{Tr}H^1(P_0)\), its Hilbert dual, and the bounded right
inverse supplied by the quotient norm.  Extend the stiffness and mass forms
in the trial slot from \(H_0^1(P_0)\) to \(H^1(P_0)\).  State the spectral,
weak conormal, reduced-DtN, and corner-amplitude maps simultaneously for the
two active families \(P_t^Y\) and \(P_t^B\); define common/jump coordinates
by a fixed invertible linear combination and define endpoint coefficients as
the leading material conormal coefficients in the fixed sector expansion.

## Reduction Witness

The corrected trace space and two-family coefficient convention are a local
replacement interface.  They do not change the frozen theorem, the completed
scalar, the super-near/coarse split, or any quantitative estimate.  Once this
typed regularity package is proved, JC_SP_LEDGER can own the finite atom list
and every Eulerian endpoint pushforward.

## Verdict

Verdict: replace locally by JC_SP_MATREG; keep route R-JC and re-audit the
replacement before drafting its proof.
